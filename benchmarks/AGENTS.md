# Benchmark scope

The root `AGENTS.md` applies here. Benchmarks must not redefine resident platform architecture.

- Doris is benchmark-only and must not become a resident service.
- Keep benchmark dependencies isolated from the default runtime.
- Record reproducible inputs, Linux/arm64 compatibility, resource impact, and cleanup steps.

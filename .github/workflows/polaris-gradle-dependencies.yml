name: Polaris 1.6.0 Gradle dependency snapshot

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/polaris-gradle-dependencies.yml
      - bootstrap/polaris/v1.6.0/source.json
      - bootstrap/polaris/v1.6.0/trusted-build-contract.json
      - bootstrap/polaris/v1.6.0/admission.json
      - bootstrap/polaris/v1.6.0/apache-polaris-release-signing-key.asc
      - scripts/package_polaris_gradle_dependencies.py
      - scripts/verify_polaris_trusted_image.py
      - tests/test_package_polaris_gradle_dependencies.py
      - tests/test_polaris_trusted_image_contract.py
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: polaris-1.6.0-gradle-dependencies
  cancel-in-progress: false

env:
  SOURCE_ARCHIVE_URL: https://downloads.apache.org/polaris/1.6.0/apache-polaris-1.6.0.tar.gz
  SOURCE_SIGNATURE_URL: https://downloads.apache.org/polaris/1.6.0/apache-polaris-1.6.0.tar.gz.asc
  SOURCE_CHECKSUM_URL: https://downloads.apache.org/polaris/1.6.0/apache-polaris-1.6.0.tar.gz.sha512
  SOURCE_ARCHIVE_SHA512: d69b1a91e16e210a78dec327fc4725983b114fbec5d86d078a3827f35fe7dd5df3e4b12d18965e5a72eace65ad224aa007004ed61c66f9abb2efafc44ceac95b
  SOURCE_SIGNATURE_SHA256: 2338e1c2385874e9bf5cf513b4d27732b1cd59e943e1662e62fa995d915e6481
  SOURCE_SIGNING_KEY_FINGERPRINT: F2EEEB06110BEE1397EC74CBB8960FF52D9B1312
  BUILDER_IMAGE: docker.io/library/gradle@sha256:cc583fa5245267fe0e1546c9989e8575473a37336ad9894dc0684a99fea1fb03
  ARTIFACT_REPOSITORY: ghcr.io/tommykammy/shirokuma-polaris-gradle-dependencies
  ARTIFACT_TYPE: application/vnd.shirokuma.polaris.gradle-dependencies.v1
  DESCRIPTOR_MEDIA_TYPE: application/vnd.shirokuma.gradle-dependency-descriptor.v1+json
  ARCHIVE_MEDIA_TYPE: application/vnd.shirokuma.gradle-cache.v1.tar+gzip
  ORAS_VERSION: 1.3.3
  ORAS_URL: https://github.com/oras-project/oras/releases/download/v1.3.3/oras_1.3.3_linux_arm64.tar.gz
  ORAS_ARCHIVE_SHA256: ac7156f93a21e903f7ad606c792f3560f17e0cd0e36365634701b1e7cc4e4eca
  COSIGN_VERSION: v3.1.1

jobs:
  resolve:
    if: >-
      github.repository == 'TommyKammy/Shirokuma' &&
      github.ref == 'refs/heads/main'
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 90
    permissions:
      contents: read
    outputs:
      active: ${{ steps.lifecycle.outputs.active }}

    steps:
      - name: Check out the reviewed dependency policy
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          persist-credentials: false

      - name: Check the dependency publication lifecycle
        id: lifecycle
        shell: bash
        run: |
          set -euo pipefail
          state=$(
            python3 -c \
              'import json; print(json.load(open("bootstrap/polaris/v1.6.0/trusted-build-contract.json"))["lifecycle"]["state"])'
          )
          case "${state}" in
            dependency_snapshot_publication_pending)
              echo "active=true" >> "${GITHUB_OUTPUT}"
              ;;
            dependency_snapshot_review_pending)
              echo "active=false" >> "${GITHUB_OUTPUT}"
              echo "dependency snapshot publisher is retired for this state"
              ;;
            *)
              echo "unexpected dependency publication lifecycle: ${state}" >&2
              exit 1
              ;;
          esac

      - name: Validate the publication-pending contract
        if: steps.lifecycle.outputs.active == 'true'
        shell: bash
        run: |
          set -euo pipefail
          python3 scripts/verify_polaris_trusted_image.py audit --root .
          python3 -m unittest -v tests.test_package_polaris_gradle_dependencies

      - name: Fetch and authenticate the ASF source release
        if: steps.lifecycle.outputs.active == 'true'
        shell: bash
        run: |
          set -euo pipefail
          source_dir="${RUNNER_TEMP}/polaris-source"
          mkdir -p "${source_dir}"
          curl --fail --location --proto '=https' --tlsv1.2 \
            "${SOURCE_ARCHIVE_URL}" \
            --output "${source_dir}/apache-polaris-1.6.0.tar.gz"
          curl --fail --location --proto '=https' --tlsv1.2 \
            "${SOURCE_SIGNATURE_URL}" \
            --output "${source_dir}/apache-polaris-1.6.0.tar.gz.asc"
          curl --fail --location --proto '=https' --tlsv1.2 \
            "${SOURCE_CHECKSUM_URL}" \
            --output "${source_dir}/apache-polaris-1.6.0.tar.gz.sha512"
          echo "${SOURCE_ARCHIVE_SHA512}  ${source_dir}/apache-polaris-1.6.0.tar.gz" \
            | sha512sum --check --strict
          echo "${SOURCE_SIGNATURE_SHA256}  ${source_dir}/apache-polaris-1.6.0.tar.gz.asc" \
            | sha256sum --check --strict
          grep --fixed-strings --quiet \
            "${SOURCE_ARCHIVE_SHA512}" \
            "${source_dir}/apache-polaris-1.6.0.tar.gz.sha512"
          export GNUPGHOME="${RUNNER_TEMP}/polaris-release-keyring"
          mkdir -m 0700 "${GNUPGHOME}"
          gpg --batch --import \
            bootstrap/polaris/v1.6.0/apache-polaris-release-signing-key.asc
          actual_fingerprint=$(
            gpg --batch --with-colons --fingerprint \
              | awk -F: '$1 == "fpr" {print $10; exit}'
          )
          test "${actual_fingerprint}" = "${SOURCE_SIGNING_KEY_FINGERPRINT}"
          gpg --batch --verify \
            "${source_dir}/apache-polaris-1.6.0.tar.gz.asc" \
            "${source_dir}/apache-polaris-1.6.0.tar.gz"
          python3 - "${source_dir}/apache-polaris-1.6.0.tar.gz" <<'PY'
          import sys
          import tarfile
          from pathlib import PurePosixPath

          archive = sys.argv[1]
          expected_root = "apache-polaris-1.6.0"
          with tarfile.open(archive, mode="r:gz") as bundle:
              members = bundle.getmembers()
          if not members:
              raise SystemExit("Polaris source archive is empty")
          for member in members:
              path = PurePosixPath(member.name)
              if (
                  path.is_absolute()
                  or not path.parts
                  or path.parts[0] != expected_root
                  or any(part in {"", ".", ".."} for part in path.parts)
                  or member.issym()
                  or member.islnk()
                  or not (member.isfile() or member.isdir())
              ):
                  raise SystemExit(
                      f"unsafe Polaris source archive member: {member.name}"
                  )
          PY

      - name: Resolve dependencies and generate strict verification metadata
        if: steps.lifecycle.outputs.active == 'true'
        shell: bash
        run: |
          set -euo pipefail
          source_archive="${RUNNER_TEMP}/polaris-source/apache-polaris-1.6.0.tar.gz"
          online_source="${RUNNER_TEMP}/polaris-online"
          gradle_home="${RUNNER_TEMP}/gradle-home"
          mkdir -p "${online_source}" "${gradle_home}"
          tar --extract --gzip --file "${source_archive}" \
            --directory "${online_source}" --strip-components 1
          docker run --rm \
            --platform linux/arm64 \
            --user "$(id -u):$(id -g)" \
            --env HOME=/tmp/gradle-user \
            --env GRADLE_USER_HOME=/gradle-home \
            --volume "${online_source}:/workspace" \
            --volume "${gradle_home}:/gradle-home" \
            --workdir /workspace \
            "${BUILDER_IMAGE}" \
            gradle \
              --no-daemon \
              --no-build-cache \
              --no-configuration-cache \
              --rerun-tasks \
              --write-verification-metadata sha256 \
              :polaris-server:assemble \
              :polaris-server:quarkusAppPartsBuild
          test -s "${online_source}/gradle/verification-metadata.xml"
          docker run --rm \
            --platform linux/arm64 \
            --user "$(id -u):$(id -g)" \
            --env HOME=/tmp/gradle-user \
            --env GRADLE_USER_HOME=/gradle-home \
            --volume "${online_source}:/workspace" \
            --volume "${gradle_home}:/gradle-home" \
            --workdir /workspace \
            "${BUILDER_IMAGE}" \
            gradle \
              --offline \
              --dependency-verification strict \
              --no-daemon \
              --no-build-cache \
              --no-configuration-cache \
              --rerun-tasks \
              :polaris-server:assemble \
              :polaris-server:quarkusAppPartsBuild

      - name: Package the canonical dependency snapshot
        if: steps.lifecycle.outputs.active == 'true'
        shell: bash
        run: |
          set -euo pipefail
          candidate_dir="${RUNNER_TEMP}/polaris-gradle-candidate"
          mkdir -p "${candidate_dir}"
          cp \
            "${RUNNER_TEMP}/polaris-online/gradle/verification-metadata.xml" \
            "${candidate_dir}/verification-metadata.xml"
          python3 scripts/package_polaris_gradle_dependencies.py create \
            --cache-root "${RUNNER_TEMP}/gradle-home" \
            --verification-metadata \
              "${candidate_dir}/verification-metadata.xml" \
            --archive \
              "${candidate_dir}/polaris-gradle-dependencies-1.6.0.tar.gz" \
            --descriptor \
              "${candidate_dir}/gradle-dependency-inputs.json"
          second_archive="${RUNNER_TEMP}/polaris-gradle-repeat.tar.gz"
          second_descriptor="${RUNNER_TEMP}/polaris-gradle-repeat.json"
          python3 scripts/package_polaris_gradle_dependencies.py create \
            --cache-root "${RUNNER_TEMP}/gradle-home" \
            --verification-metadata \
              "${candidate_dir}/verification-metadata.xml" \
            --archive "${second_archive}" \
            --descriptor "${second_descriptor}"
          cmp \
            "${candidate_dir}/polaris-gradle-dependencies-1.6.0.tar.gz" \
            "${second_archive}"
          cmp "${candidate_dir}/gradle-dependency-inputs.json" \
            "${second_descriptor}"

      - name: Prove a fresh network-none offline server build
        if: steps.lifecycle.outputs.active == 'true'
        shell: bash
        run: |
          set -euo pipefail
          candidate_dir="${RUNNER_TEMP}/polaris-gradle-candidate"
          offline_home="${RUNNER_TEMP}/gradle-home-offline"
          offline_source="${RUNNER_TEMP}/polaris-offline"
          mkdir -p "${offline_source}"
          python3 scripts/package_polaris_gradle_dependencies.py verify \
            --descriptor \
              "${candidate_dir}/gradle-dependency-inputs.json" \
            --verification-metadata \
              "${candidate_dir}/verification-metadata.xml" \
            --archive \
              "${candidate_dir}/polaris-gradle-dependencies-1.6.0.tar.gz" \
            --extract-root "${offline_home}"
          cmp "${candidate_dir}/verification-metadata.xml" \
            "${offline_home}/verification-metadata.xml"
          tar --extract --gzip \
            --file \
              "${RUNNER_TEMP}/polaris-source/apache-polaris-1.6.0.tar.gz" \
            --directory "${offline_source}" --strip-components 1
          install -m 0644 \
            "${offline_home}/verification-metadata.xml" \
            "${offline_source}/gradle/verification-metadata.xml"
          rm "${offline_home}/verification-metadata.xml"
          docker run --rm \
            --platform linux/arm64 \
            --network none \
            --user "$(id -u):$(id -g)" \
            --env HOME=/tmp/gradle-user \
            --env GRADLE_USER_HOME=/gradle-home \
            --volume "${offline_source}:/workspace" \
            --volume "${offline_home}:/gradle-home" \
            --workdir /workspace \
            "${BUILDER_IMAGE}" \
            gradle \
              --offline \
              --dependency-verification strict \
              --no-daemon \
              --no-build-cache \
              --no-configuration-cache \
              --rerun-tasks \
              :polaris-server:assemble \
              :polaris-server:quarkusAppPartsBuild
          CANDIDATE_DIR="${candidate_dir}" python3 - <<'PY'
          import json
          import os
          from pathlib import Path

          candidate = Path(os.environ["CANDIDATE_DIR"])
          descriptor = json.loads(
              (candidate / "gradle-dependency-inputs.json").read_text(
                  encoding="utf-8"
              )
          )
          evidence = {
              "schema_version": 1,
              "result": "passed",
              "platform": "linux/arm64",
              "network": "none",
              "gradle_offline": True,
              "dependency_verification": "strict",
              "build_cache": False,
              "configuration_cache": False,
              "tasks": [
                  ":polaris-server:assemble",
                  ":polaris-server:quarkusAppPartsBuild",
              ],
              "descriptor_sha256": __import__("hashlib").sha256(
                  (candidate / "gradle-dependency-inputs.json").read_bytes()
              ).hexdigest(),
              "archive_sha256": descriptor["archive"]["sha256"],
              "verification_metadata_sha256": descriptor[
                  "verification_metadata"
              ]["sha256"],
          }
          (candidate / "offline-build.json").write_text(
              json.dumps(evidence, indent=2, sort_keys=True) + "\n",
              encoding="utf-8",
          )
          PY

      - name: Record the dependency resolver toolchain
        if: steps.lifecycle.outputs.active == 'true'
        shell: bash
        run: |
          set -euo pipefail
          candidate_dir="${RUNNER_TEMP}/polaris-gradle-candidate"
          docker_version=$(docker version --format '{{.Client.Version}}')
          builder_gradle=$(
            docker run --rm --platform linux/arm64 \
              "${BUILDER_IMAGE}" gradle --version \
              | awk '/^Gradle / {print $2}'
          )
          builder_java=$(
            docker run --rm --platform linux/arm64 \
              "${BUILDER_IMAGE}" java -version 2>&1 \
              | awk -F'"' '/version/ {print $2; exit}'
          )
          DOCKER_VERSION="${docker_version}" \
          BUILDER_GRADLE="${builder_gradle}" \
          BUILDER_JAVA="${builder_java}" \
          CANDIDATE_DIR="${candidate_dir}" \
          python3 - <<'PY'
          import json
          import os
          import platform
          import subprocess
          from pathlib import Path

          def version(command):
              return subprocess.run(
                  command,
                  check=True,
                  capture_output=True,
                  text=True,
              ).stdout.splitlines()[0]

          evidence = {
              "schema_version": 1,
              "runner": "ubuntu-24.04-arm",
              "platform": platform.platform(),
              "builder_image": os.environ["BUILDER_IMAGE"],
              "gradle": os.environ["BUILDER_GRADLE"],
              "java": os.environ["BUILDER_JAVA"],
              "docker": os.environ["DOCKER_VERSION"],
              "python": platform.python_version(),
              "gpg": version(["gpg", "--version"]),
              "curl": version(["curl", "--version"]),
              "tar": version(["tar", "--version"]),
          }
          Path(os.environ["CANDIDATE_DIR"], "toolchain.json").write_text(
              json.dumps(evidence, indent=2, sort_keys=True) + "\n",
              encoding="utf-8",
          )
          PY

      - name: Retain the verified candidate for publication
        if: steps.lifecycle.outputs.active == 'true'
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: polaris-gradle-candidate-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            ${{ runner.temp }}/polaris-gradle-candidate/gradle-dependency-inputs.json
            ${{ runner.temp }}/polaris-gradle-candidate/offline-build.json
            ${{ runner.temp }}/polaris-gradle-candidate/polaris-gradle-dependencies-1.6.0.tar.gz
            ${{ runner.temp }}/polaris-gradle-candidate/toolchain.json
            ${{ runner.temp }}/polaris-gradle-candidate/verification-metadata.xml
          if-no-files-found: error
          compression-level: 0
          retention-days: 1

  verify:
    needs: resolve
    if: >-
      github.repository == 'TommyKammy/Shirokuma' &&
      github.ref == 'refs/heads/main' &&
      needs.resolve.result == 'success' &&
      needs.resolve.outputs.active == 'true'
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 30
    permissions:
      contents: read
    outputs:
      archive_sha256: ${{ steps.candidate.outputs.archive_sha256 }}
      descriptor_sha256: ${{ steps.candidate.outputs.descriptor_sha256 }}
      metadata_sha256: ${{ steps.candidate.outputs.metadata_sha256 }}
      offline_sha256: ${{ steps.candidate.outputs.offline_sha256 }}
      toolchain_sha256: ${{ steps.candidate.outputs.toolchain_sha256 }}

    steps:
      - name: Check out the reviewed verification policy
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          persist-credentials: false

      - name: Download the exact candidate into the read-only verifier
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: polaris-gradle-candidate-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/polaris-gradle-candidate

      - name: Reverify the publication candidate without write credentials
        id: candidate
        shell: bash
        run: |
          set -euo pipefail
          candidate_dir="${RUNNER_TEMP}/polaris-gradle-candidate"
          python3 scripts/verify_polaris_trusted_image.py audit --root .
          python3 scripts/package_polaris_gradle_dependencies.py verify \
            --descriptor \
              "${candidate_dir}/gradle-dependency-inputs.json" \
            --verification-metadata \
              "${candidate_dir}/verification-metadata.xml" \
            --archive \
              "${candidate_dir}/polaris-gradle-dependencies-1.6.0.tar.gz"
          CANDIDATE_DIR="${candidate_dir}" python3 - <<'PY'
          import hashlib
          import json
          import os
          import re
          from pathlib import Path

          candidate = Path(os.environ["CANDIDATE_DIR"])
          expected_files = {
              "gradle-dependency-inputs.json",
              "offline-build.json",
              "polaris-gradle-dependencies-1.6.0.tar.gz",
              "toolchain.json",
              "verification-metadata.xml",
          }
          if (
              {path.name for path in candidate.iterdir()} != expected_files
              or any(not path.is_file() or path.is_symlink()
                      for path in candidate.iterdir())
          ):
              raise SystemExit("candidate artifact inventory changed")

          def sha256(name):
              return hashlib.sha256(
                  (candidate / name).read_bytes()
              ).hexdigest()

          descriptor_sha256 = sha256("gradle-dependency-inputs.json")
          archive_sha256 = sha256(
              "polaris-gradle-dependencies-1.6.0.tar.gz"
          )
          metadata_sha256 = sha256("verification-metadata.xml")
          offline = json.loads(
              (candidate / "offline-build.json").read_text(encoding="utf-8")
          )
          expected_offline_keys = {
              "archive_sha256",
              "build_cache",
              "configuration_cache",
              "dependency_verification",
              "descriptor_sha256",
              "gradle_offline",
              "network",
              "platform",
              "result",
              "schema_version",
              "tasks",
              "verification_metadata_sha256",
          }
          if set(offline) != expected_offline_keys:
              raise SystemExit("offline evidence schema changed")
          expected_offline = {
              "schema_version": 1,
              "result": "passed",
              "platform": "linux/arm64",
              "network": "none",
              "gradle_offline": True,
              "dependency_verification": "strict",
              "build_cache": False,
              "configuration_cache": False,
              "tasks": [
                  ":polaris-server:assemble",
                  ":polaris-server:quarkusAppPartsBuild",
              ],
              "descriptor_sha256": descriptor_sha256,
              "archive_sha256": archive_sha256,
              "verification_metadata_sha256": metadata_sha256,
          }
          if offline != expected_offline:
              raise SystemExit("offline evidence differs from candidate bytes")

          toolchain = json.loads(
              (candidate / "toolchain.json").read_text(encoding="utf-8")
          )
          if set(toolchain) != {
              "builder_image",
              "curl",
              "docker",
              "gpg",
              "gradle",
              "java",
              "platform",
              "python",
              "runner",
              "schema_version",
              "tar",
          }:
              raise SystemExit("toolchain evidence schema changed")
          if (
              toolchain["schema_version"] != 1
              or toolchain["runner"] != "ubuntu-24.04-arm"
              or toolchain["builder_image"] != os.environ["BUILDER_IMAGE"]
              or toolchain["gradle"] != "9.6.0"
              or not re.fullmatch(r"21(?:\.[0-9]+)+", toolchain["java"])
              or not re.search(
                  r"linux.*(?:aarch64|arm64)",
                  toolchain["platform"],
                  re.IGNORECASE,
              )
              or any(
                  not isinstance(toolchain[field], str)
                  or not toolchain[field]
                  for field in ["curl", "docker", "gpg", "python", "tar"]
              )
          ):
              raise SystemExit("observed toolchain differs from contract")

          outputs = {
              "archive_sha256": archive_sha256,
              "descriptor_sha256": descriptor_sha256,
              "metadata_sha256": metadata_sha256,
              "offline_sha256": sha256("offline-build.json"),
              "toolchain_sha256": sha256("toolchain.json"),
          }
          with Path(os.environ["GITHUB_OUTPUT"]).open(
              "a",
              encoding="utf-8",
          ) as stream:
              for name, value in sorted(outputs.items()):
                  stream.write(f"{name}={value}\n")
          PY

  publish:
    needs:
      - resolve
      - verify
    if: >-
      github.repository == 'TommyKammy/Shirokuma' &&
      github.ref == 'refs/heads/main' &&
      needs.resolve.outputs.active == 'true' &&
      needs.verify.result == 'success'
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 30
    permissions:
      attestations: write
      contents: read
      id-token: write
      packages: write

    steps:
      - name: Check out the reviewed publication policy
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6
        with:
          persist-credentials: false

      - name: Download the exact read-only-verified candidate
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: polaris-gradle-candidate-${{ github.run_id }}-${{ github.run_attempt }}
          path: ${{ runner.temp }}/polaris-gradle-candidate

      - name: Bind candidate bytes to the read-only verification
        shell: bash
        run: |
          set -euo pipefail
          candidate_dir="${RUNNER_TEMP}/polaris-gradle-candidate"
          echo "${{ needs.verify.outputs.descriptor_sha256 }}  ${candidate_dir}/gradle-dependency-inputs.json" \
            | sha256sum --check --strict
          echo "${{ needs.verify.outputs.archive_sha256 }}  ${candidate_dir}/polaris-gradle-dependencies-1.6.0.tar.gz" \
            | sha256sum --check --strict
          echo "${{ needs.verify.outputs.metadata_sha256 }}  ${candidate_dir}/verification-metadata.xml" \
            | sha256sum --check --strict
          echo "${{ needs.verify.outputs.offline_sha256 }}  ${candidate_dir}/offline-build.json" \
            | sha256sum --check --strict
          echo "${{ needs.verify.outputs.toolchain_sha256 }}  ${candidate_dir}/toolchain.json" \
            | sha256sum --check --strict

      - name: Install checksum-pinned ORAS
        shell: bash
        run: |
          set -euo pipefail
          archive="${RUNNER_TEMP}/oras.tar.gz"
          install_dir="${RUNNER_TEMP}/oras-bin"
          mkdir -p "${install_dir}"
          curl --fail --location --proto '=https' --tlsv1.2 \
            "${ORAS_URL}" --output "${archive}"
          echo "${ORAS_ARCHIVE_SHA256}  ${archive}" \
            | sha256sum --check --strict
          tar --extract --gzip --file "${archive}" \
            --directory "${install_dir}" oras
          chmod 0755 "${install_dir}/oras"
          echo "${install_dir}" >> "${GITHUB_PATH}"
          test "$("${install_dir}/oras" version | awk '/Version:/ {print $2}')" \
            = "${ORAS_VERSION}"

      - name: Install pinned Cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2
        with:
          cosign-release: ${{ env.COSIGN_VERSION }}

      - name: Publish the run-scoped immutable OCI artifact
        id: push
        env:
          GHCR_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          candidate_dir="${RUNNER_TEMP}/polaris-gradle-candidate"
          tag="${ARTIFACT_REPOSITORY}:1.6.0-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          created=$(git show -s --no-show-signature --format=%cI "${GITHUB_SHA}")
          if ! [[ "${created}" =~ ^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}([+-][0-9]{2}:[0-9]{2}|Z)$ ]]; then
            echo "git committer date is not strict RFC 3339" >&2
            exit 1
          fi
          echo "${GHCR_TOKEN}" \
            | oras login ghcr.io --username "${GITHUB_ACTOR}" --password-stdin
          set +e
          resolve_error=$(oras resolve "${tag}" 2>&1)
          resolve_status=$?
          set -e
          if [ "${resolve_status}" -eq 0 ]; then
            echo "run-scoped OCI tag already exists; refusing overwrite" >&2
            exit 1
          fi
          if ! grep --extended-regexp --ignore-case --quiet \
            '(manifest unknown|name unknown|not found|status code: 404)' \
            <<< "${resolve_error}"; then
            echo "cannot prove that the run-scoped OCI tag is absent" >&2
            printf '%s\n' "${resolve_error}" >&2
            exit 1
          fi
          oras push \
            --image-spec v1.1 \
            --artifact-type "${ARTIFACT_TYPE}" \
            --annotation \
              "org.opencontainers.image.source=https://github.com/${GITHUB_REPOSITORY}" \
            --annotation "org.opencontainers.image.revision=${GITHUB_SHA}" \
            --annotation "org.opencontainers.image.created=${created}" \
            --export-manifest "${candidate_dir}/oci-manifest.json" \
            "${tag}" \
            "${candidate_dir}/gradle-dependency-inputs.json:${DESCRIPTOR_MEDIA_TYPE}" \
            "${candidate_dir}/polaris-gradle-dependencies-1.6.0.tar.gz:${ARCHIVE_MEDIA_TYPE}"
          digest=$(oras resolve "${tag}")
          case "${digest}" in
            sha256:????????????????????????????????????????????????????????????????)
              ;;
            *)
              echo "ORAS returned an invalid manifest digest" >&2
              exit 1
              ;;
          esac
          echo "PUBLISHED_TAG=${tag}" >> "${GITHUB_ENV}"
          echo "PUBLISHED_CREATED=${created}" >> "${GITHUB_ENV}"
          echo "PUBLISHED_DIGEST=${digest}" >> "${GITHUB_ENV}"
          echo "PUBLISHED_REFERENCE=${ARTIFACT_REPOSITORY}@${digest}" \
            >> "${GITHUB_ENV}"
          echo "digest=${digest}" >> "${GITHUB_OUTPUT}"
          echo "reference=${ARTIFACT_REPOSITORY}@${digest}" \
            >> "${GITHUB_OUTPUT}"
          echo "tag=${tag}" >> "${GITHUB_OUTPUT}"

      - name: Bind the raw OCI manifest to both artifact layers
        shell: bash
        run: |
          set -euo pipefail
          candidate_dir="${RUNNER_TEMP}/polaris-gradle-candidate"
          CANDIDATE_DIR="${candidate_dir}" python3 - <<'PY'
          import hashlib
          import json
          import os
          import re
          from pathlib import Path

          candidate = Path(os.environ["CANDIDATE_DIR"])
          manifest_path = candidate / "oci-manifest.json"
          manifest_bytes = manifest_path.read_bytes()
          manifest = json.loads(manifest_bytes)
          digest = "sha256:" + hashlib.sha256(manifest_bytes).hexdigest()
          if digest != os.environ["PUBLISHED_DIGEST"]:
              raise SystemExit("raw OCI manifest digest differs from ORAS")
          if set(manifest) != {
              "schemaVersion",
              "mediaType",
              "artifactType",
              "config",
              "layers",
              "annotations",
          }:
              raise SystemExit("OCI manifest root is not closed-world")
          if (
              manifest["schemaVersion"] != 2
              or manifest["mediaType"]
              != "application/vnd.oci.image.manifest.v1+json"
              or manifest["artifactType"] != os.environ["ARTIFACT_TYPE"]
          ):
              raise SystemExit("OCI manifest identity differs from contract")
          config = manifest["config"]
          if (
              set(config) != {"mediaType", "digest", "size", "data"}
              or manifest["annotations"]
              != {
                  "org.opencontainers.image.created": os.environ[
                      "PUBLISHED_CREATED"
                  ],
                  "org.opencontainers.image.revision": os.environ[
                      "GITHUB_SHA"
                  ],
                  "org.opencontainers.image.source": (
                      "https://github.com/"
                      + os.environ["GITHUB_REPOSITORY"]
                  ),
              }
              or config.get("mediaType")
              != "application/vnd.oci.empty.v1+json"
              or config.get("digest")
              != "sha256:44136fa355b3678a1146ad16f7e8649e94fb4fc21fe77e8310c060f61caaff8a"
              or config.get("size") != 2
              or config.get("data") != "e30="
          ):
              raise SystemExit("OCI empty config differs from contract")
          expected = [
              (
                  candidate / "gradle-dependency-inputs.json",
                  os.environ["DESCRIPTOR_MEDIA_TYPE"],
              ),
              (
                  candidate / "polaris-gradle-dependencies-1.6.0.tar.gz",
                  os.environ["ARCHIVE_MEDIA_TYPE"],
              ),
          ]
          layers = manifest.get("layers")
          if not isinstance(layers, list) or len(layers) != len(expected):
              raise SystemExit("OCI artifact must contain exactly two layers")
          for layer, (path, media_type) in zip(layers, expected):
              if set(layer) != {
                  "mediaType",
                  "digest",
                  "size",
                  "annotations",
              }:
                  raise SystemExit("OCI layer descriptor is not closed-world")
              expected_digest = (
                  "sha256:" + hashlib.sha256(path.read_bytes()).hexdigest()
              )
              if (
                  layer["mediaType"] != media_type
                  or layer["digest"] != expected_digest
                  or layer["size"] != path.stat().st_size
                  or layer["annotations"]
                  != {"org.opencontainers.image.title": path.name}
              ):
                  raise SystemExit(
                      f"OCI layer differs from retained bytes: {path.name}"
                  )
          if not re.fullmatch(r"sha256:[0-9a-f]{64}", digest):
              raise SystemExit("OCI manifest digest is invalid")
          PY

      - name: Keyless-sign the exact OCI manifest
        env:
          GHCR_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          candidate_dir="${RUNNER_TEMP}/polaris-gradle-candidate"
          echo "${GHCR_TOKEN}" \
            | oras login ghcr.io --username "${GITHUB_ACTOR}" --password-stdin
          cosign sign --yes \
            --bundle "${candidate_dir}/cosign-signature-bundle.json" \
            "${PUBLISHED_REFERENCE}"
          cosign verify \
            --bundle "${candidate_dir}/cosign-signature-bundle.json" \
            --certificate-identity \
              "https://github.com/${GITHUB_REPOSITORY}/.github/workflows/polaris-gradle-dependencies.yml@${GITHUB_REF}" \
            --certificate-oidc-issuer \
              "https://token.actions.githubusercontent.com" \
            "${PUBLISHED_REFERENCE}" \
            > "${candidate_dir}/cosign-verify.json"

      - name: Publish SLSA provenance for the exact OCI manifest
        id: provenance
        uses: actions/attest-build-provenance@977bb373ede98d70efdf65b84cb5f73e068dcc2a # v3
        with:
          subject-name: ${{ env.ARTIFACT_REPOSITORY }}
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true

      - name: Prove anonymous exact-digest retrieval
        shell: bash
        run: |
          set -euo pipefail
          candidate_dir="${RUNNER_TEMP}/polaris-gradle-candidate"
          oras logout ghcr.io
          anonymous_config="${RUNNER_TEMP}/anonymous-registry-config.json"
          anonymous_dir="${RUNNER_TEMP}/anonymous-pull"
          printf '{}\n' > "${anonymous_config}"
          mkdir -p "${anonymous_dir}"
          oras pull \
            --registry-config "${anonymous_config}" \
            --output "${anonymous_dir}" \
            "${PUBLISHED_REFERENCE}"
          cmp "${candidate_dir}/gradle-dependency-inputs.json" \
            "${anonymous_dir}/gradle-dependency-inputs.json"
          cmp "${candidate_dir}/polaris-gradle-dependencies-1.6.0.tar.gz" \
            "${anonymous_dir}/polaris-gradle-dependencies-1.6.0.tar.gz"

      - name: Verify the exact workflow provenance
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          candidate_dir="${RUNNER_TEMP}/polaris-gradle-candidate"
          gh attestation verify "oci://${PUBLISHED_REFERENCE}" \
            --repo "${GITHUB_REPOSITORY}" \
            --predicate-type "https://slsa.dev/provenance/v1" \
            --signer-workflow \
              "${GITHUB_REPOSITORY}/.github/workflows/polaris-gradle-dependencies.yml" \
            --signer-digest "${GITHUB_WORKFLOW_SHA}" \
            --source-digest "${GITHUB_SHA}" \
            --source-ref "${GITHUB_REF}" \
            --format json \
            > "${candidate_dir}/slsa-verify.json"

      - name: Record the review-pending publication
        shell: bash
        run: |
          set -euo pipefail
          candidate_dir="${RUNNER_TEMP}/polaris-gradle-candidate"
          CANDIDATE_DIR="${candidate_dir}" python3 - <<'PY'
          import hashlib
          import json
          import os
          from pathlib import Path

          candidate = Path(os.environ["CANDIDATE_DIR"])

          def file_record(name):
              path = candidate / name
              return {
                  "filename": name,
                  "sha256": hashlib.sha256(path.read_bytes()).hexdigest(),
                  "size": path.stat().st_size,
              }

          record = {
              "schema_version": 1,
              "state": "dependency_snapshot_review_pending",
              "reference": os.environ["PUBLISHED_REFERENCE"],
              "tag": os.environ["PUBLISHED_TAG"],
              "created": os.environ["PUBLISHED_CREATED"],
              "manifest": file_record("oci-manifest.json"),
              "descriptor": file_record(
                  "gradle-dependency-inputs.json"
              ),
              "archive": file_record(
                  "polaris-gradle-dependencies-1.6.0.tar.gz"
              ),
              "verification_metadata": file_record(
                  "verification-metadata.xml"
              ),
              "offline_build": file_record("offline-build.json"),
              "toolchain": file_record("toolchain.json"),
              "cosign_bundle": file_record(
                  "cosign-signature-bundle.json"
              ),
              "cosign_verification": file_record("cosign-verify.json"),
              "slsa_verification": file_record("slsa-verify.json"),
              "workflow": {
                  "repository": os.environ["GITHUB_REPOSITORY"],
                  "ref": os.environ["GITHUB_REF"],
                  "source_sha": os.environ["GITHUB_SHA"],
                  "workflow_sha": os.environ["GITHUB_WORKFLOW_SHA"],
                  "run_id": os.environ["GITHUB_RUN_ID"],
                  "run_attempt": os.environ["GITHUB_RUN_ATTEMPT"],
                  "event": os.environ["GITHUB_EVENT_NAME"],
              },
              "anonymous_pull": True,
              "admitted": False,
          }
          (candidate / "publication.json").write_text(
              json.dumps(record, indent=2, sort_keys=True) + "\n",
              encoding="utf-8",
          )
          PY

      - name: Retain review-pending publication evidence
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: polaris-gradle-publication-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            ${{ runner.temp }}/polaris-gradle-candidate/cosign-signature-bundle.json
            ${{ runner.temp }}/polaris-gradle-candidate/cosign-verify.json
            ${{ runner.temp }}/polaris-gradle-candidate/gradle-dependency-inputs.json
            ${{ runner.temp }}/polaris-gradle-candidate/oci-manifest.json
            ${{ runner.temp }}/polaris-gradle-candidate/offline-build.json
            ${{ runner.temp }}/polaris-gradle-candidate/publication.json
            ${{ runner.temp }}/polaris-gradle-candidate/slsa-verify.json
            ${{ runner.temp }}/polaris-gradle-candidate/toolchain.json
            ${{ runner.temp }}/polaris-gradle-candidate/verification-metadata.xml
          if-no-files-found: error
          retention-days: 90

      - name: Remove registry credentials
        if: always()
        continue-on-error: true
        shell: bash
        run: oras logout ghcr.io
